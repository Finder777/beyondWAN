{
  "routes": [
    {
      "route": "/*",
      "allowedRoles": ["authenticated"],
      "statusCode": 401,
      "redirect": "/.auth/login/aad"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html"
  }
}