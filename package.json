{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build": "mkdir -p build && cpr src/ build/ && cp staticwebapp.config.json build/"
  },
  "keywords": [
    "egrDigital"
  ],
  "author": "SnakeByte",
  "devDependencies": {
    "fs-extra": "^11.3.0"
  }
}
